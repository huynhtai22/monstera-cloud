import { Copy, Database, KeyRound, Plus, Trash2 } from "lucide-react";
import { toast } from "sonner";

export function ApiKeysTab({ apiKeys, newlyGeneratedKey, isGenerating, canManage, handleGenerateKey, handleDeleteKey }: { apiKeys: any[]; newlyGeneratedKey: string | null; isGenerating: boolean; canManage: boolean; handleGenerateKey: () => Promise<void>; handleDeleteKey: (id: string) => Promise<void> }) {
  const copy = (value: string) => { void navigator.clipboard.writeText(value); toast.success("Copied"); };
  return <div className="max-w-4xl space-y-6"><div><h3 className="flex items-center text-lg font-semibold"><KeyRound className="mr-2 h-5 w-5 text-cyan-600" />API keys</h3><p className="mt-1 text-sm text-slate-500">Workspace-scoped bearer credentials. New secrets are shown only once.</p></div>{newlyGeneratedKey ? <div className="rounded-xl border border-emerald-200 bg-emerald-50 p-4"><p className="font-semibold text-emerald-900">Copy this key now</p><div className="mt-3 flex gap-2"><code className="min-w-0 flex-1 break-all rounded bg-white p-3 text-xs">{newlyGeneratedKey}</code><button onClick={() => copy(newlyGeneratedKey)} aria-label="Copy API key" className="rounded border border-emerald-300 bg-white px-3"><Copy className="h-4 w-4" /></button></div></div> : null}<div className="rounded-xl border border-slate-200 bg-white p-5 dark:border-slate-800 dark:bg-slate-900"><div className="mb-4 flex items-center justify-between"><h4 className="text-sm font-medium">Active keys</h4>{canManage ? <button onClick={handleGenerateKey} disabled={isGenerating} className="flex items-center rounded-lg bg-cyan-700 px-3 py-2 text-sm font-semibold text-white disabled:opacity-60"><Plus className="mr-2 h-4 w-4" />Generate</button> : null}</div><div className="divide-y divide-slate-200 dark:divide-slate-800">{apiKeys.map((key) => <div key={key.id} className="flex items-center justify-between py-4"><div className="flex items-center gap-3"><Database className="h-4 w-4 text-slate-400" /><div><p className="text-sm font-medium">{key.name}</p><code className="text-xs text-slate-500">{key.keyMasked}</code></div></div>{canManage ? <button onClick={() => handleDeleteKey(key.id)} aria-label={`Revoke ${key.name}`} className="p-2 text-slate-400 hover:text-red-600"><Trash2 className="h-4 w-4" /></button> : null}</div>)}{!apiKeys.length ? <p className="py-8 text-center text-sm text-slate-500">No active API keys.</p> : null}</div></div></div>;
}
