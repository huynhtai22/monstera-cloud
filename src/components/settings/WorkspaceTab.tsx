import { Building2, CheckCircle2 } from "lucide-react";

export function WorkspaceTab({ activeWorkspace }: { activeWorkspace: any }) {
  if (!activeWorkspace) return <p className="text-sm text-ink-mute">Select a workspace to continue.</p>;
  return <div className="max-w-3xl space-y-6"><div><h3 className="flex items-center text-lg font-semibold text-ink"><Building2 className="mr-2 h-5 w-5 text-ink-mute" strokeWidth={1.5} />Workspace</h3><p className="mt-1 text-sm text-ink-mute">Tenant identity, access and certified providers for this agency.</p></div><dl className="grid gap-4 rounded-lg border border-line bg-canvas p-6 sm:grid-cols-2"><div><dt className="font-mono text-[10px] uppercase tracking-[0.14em] text-ink-mute">Agency</dt><dd className="mt-1 font-medium text-ink">{activeWorkspace.name}</dd></div><div><dt className="font-mono text-[10px] uppercase tracking-[0.14em] text-ink-mute">Slug</dt><dd className="mt-1 font-mono text-sm text-ink">{activeWorkspace.slug}</dd></div><div><dt className="font-mono text-[10px] uppercase tracking-[0.14em] text-ink-mute">Your role</dt><dd className="mt-1 capitalize text-ink">{activeWorkspace.role}</dd></div><div><dt className="font-mono text-[10px] uppercase tracking-[0.14em] text-ink-mute">Status</dt><dd className="mt-1 capitalize text-ink">{String(activeWorkspace.status).toLowerCase()}</dd></div></dl><div className="rounded-lg border border-line bg-canvas p-6"><h4 className="font-medium text-ink">Enabled providers</h4><div className="mt-3 flex flex-wrap gap-2">{activeWorkspace.enabledProviders?.map((provider: string) => <span key={provider} className="flex items-center gap-1 rounded-md border border-line px-3 py-1 text-xs text-ink"><CheckCircle2 className="h-3.5 w-3.5 text-accent" strokeWidth={1.5} />{provider.replaceAll("_", " ")}</span>)}</div></div></div>;
}
