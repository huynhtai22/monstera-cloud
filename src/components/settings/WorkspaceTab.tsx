import { Building2, CheckCircle2 } from "lucide-react";

export function WorkspaceTab({ activeWorkspace }: { activeWorkspace: any }) {
  if (!activeWorkspace) return <p className="text-sm text-slate-500">Select a workspace to continue.</p>;
  return <div className="max-w-3xl space-y-6"><div><h3 className="flex items-center text-lg font-semibold"><Building2 className="mr-2 h-5 w-5 text-cyan-600" />Workspace</h3><p className="mt-1 text-sm text-slate-500">Tenant identity, access and certified providers for this agency.</p></div><dl className="grid gap-4 rounded-xl border border-slate-200 bg-white p-6 sm:grid-cols-2 dark:border-slate-800 dark:bg-slate-900"><div><dt className="text-xs uppercase tracking-wide text-slate-500">Agency</dt><dd className="mt-1 font-medium">{activeWorkspace.name}</dd></div><div><dt className="text-xs uppercase tracking-wide text-slate-500">Slug</dt><dd className="mt-1 font-mono text-sm">{activeWorkspace.slug}</dd></div><div><dt className="text-xs uppercase tracking-wide text-slate-500">Your role</dt><dd className="mt-1 capitalize">{activeWorkspace.role}</dd></div><div><dt className="text-xs uppercase tracking-wide text-slate-500">Status</dt><dd className="mt-1 capitalize">{String(activeWorkspace.status).toLowerCase()}</dd></div></dl><div className="rounded-xl border border-slate-200 bg-white p-6 dark:border-slate-800 dark:bg-slate-900"><h4 className="font-medium">Enabled providers</h4><div className="mt-3 flex flex-wrap gap-2">{activeWorkspace.enabledProviders?.map((provider: string) => <span key={provider} className="flex items-center gap-1 rounded-full bg-emerald-50 px-3 py-1 text-xs text-emerald-800"><CheckCircle2 className="h-3.5 w-3.5" />{provider.replaceAll("_", " ")}</span>)}</div></div></div>;
}
