# Environment Configuration

# Database (Neon Postgres)
DATABASE_URL="postgresql://user:pass@host/db?sslmode=require"

# NextAuth
NEXTAUTH_SECRET="your-secret-here"
NEXTAUTH_URL="http://localhost:3000"

# Google OAuth (Optional for social login)
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# Resend (Email Verification)
# Get your API key at https://resend.com
RESEND_API_KEY="re_..."

# Xendit (Invoices / checkout) — set for Production + Preview in Vercel
XENDIT_SECRET_KEY="xnd_development_..."
# Optional: verify invoice webhooks (Dashboard → Webhooks → callback token)
XENDIT_WEBHOOK_TOKEN=""
# USD default; use IDR if your Xendit account invoices in IDR only
XENDIT_DEFAULT_CURRENCY="USD"
# When XENDIT_DEFAULT_CURRENCY=IDR, USD list prices are converted (tune rate as needed)
XENDIT_IDR_PER_USD="16500"
